<h5>Search result for term <em>'<%= @query %>'</em> in Criminals resulted in <%= pluralize(@total_hits, 'hit') %></h5>
<p class="supertiny">&nbsp;</p>

<% unless @criminals.empty? %>
  <p><em><strong>Criminals matching term '<%= @query %>'</strong></em></p>
  <ul>
    <% for criminal in @criminals do %>
      <li><%= link_to criminal.name, criminal_path(criminal) %></li>
    <% end %>
  </ul>
  <p class="smtext">&nbsp;</p>
<% end %>
<%= link_to 'Back', :back %>